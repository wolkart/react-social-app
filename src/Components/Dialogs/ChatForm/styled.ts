import styled from 'styled-components';

export const StyledChatForm = styled.form`
  display: flex;
  column-gap: 10px;
  
  textarea {
    padding-left: 20px;
    flex: 1 1 auto;
    background-color: #f5f5f5;
    border-color: #f5f5f5;
    border-radius: 10px;
  }
`